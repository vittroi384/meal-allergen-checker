# 급식 알레르기 판별 시스템 — 요구사항 명세 (SPEC)

> 최종 수정: 2026-08-31
> 상태: 초안 (구현 전 확정용)

## 1. 목적

학교 영양(교)사·담당 교사가 **학생 알레르기 정보**와 **매일 급식 메뉴**를 자동으로 대조하여
"오늘 / 이번 주 / 이번 달 급식을 못 먹는 학생"을 즉시 확인하고, 알림까지 받을 수 있는 시스템.

### 핵심 제약
- **비용 0원**: 유료 서버·DB 없음. Google 무료 서비스만 사용 (문자 채널만 선택적 유료).
- **설치 없음**: 학교 담당자는 웹앱 URL 클릭 + 비밀번호 입력만으로 사용.
- **운영 계정 1개**: 전용 Google 계정 하나에 시트 + Apps Script + 웹앱을 모두 배포.
- **비개발자 배포 가능**: README만 보고 계정 생성부터 배포까지 따라 할 수 있어야 함.

## 2. 아키텍처

| 역할 | 구현 |
|---|---|
| 데이터베이스 | Google Sheets (시트 4개 + 템플릿 시트) |
| 백엔드 / 스케줄러 | Google Apps Script (시간 기반 트리거) |
| UI | Apps Script `HtmlService` 웹앱 (한국어, 반응형) |
| 외부 데이터 | NEIS 교육정보 개방포털 Open API (`mealServiceDietInfo`, `schoolInfo`) |
| 알림 | MailApp(이메일), 솔라피/알리고(문자), 텔레그램 봇 — 플러그인 구조 |
| 로컬 개발 | `clasp` 로 로컬 폴더 ↔ Apps Script 프로젝트 동기화 |
| 테스트 | 순수 JS 로직을 Node 로 실행 (`node --test`) |

### 설계 원칙
- **순수 로직 분리**: 메뉴 파싱, 알레르기 판별, 알림 문구 생성은 Apps Script 전역 객체
  (`SpreadsheetApp`, `UrlFetchApp`, `MailApp` 등)에 **의존하지 않는** 순수 함수로 작성.
  같은 파일이 Apps Script 와 Node 양쪽에서 동작해야 함 (UMD 스타일 export 가드).
- **비밀값은 코드에 없음**: NEIS 인증키, 문자 API 키, 텔레그램 토큰, 접속 비밀번호는
  `PropertiesService.getScriptProperties()` 에만 저장. 설정 화면에서 입력받음.
  `.clasp.json` 은 `.gitignore` 에 포함.
- **시간대**: `Asia/Seoul` 고정 (appsscript.json `timeZone`). "오늘"·"내일"·"이번 주" 계산은 전부
  `Utilities.formatDate(new Date(), 'Asia/Seoul', ...)` 로 구한 날짜 문자열 기준. 순수 함수는 `Date` 객체 대신
  `yyyy-MM-dd` 문자열을 입력받아 시간대 문제를 원천 차단.
- **트리거 시각은 근사치**: Apps Script 시간 트리거는 지정 시각 ±15분 오차가 있음. 설정 화면과 README 에 명시.
- **멱등성**: 동기화·알림·setup 은 여러 번 실행해도 안전해야 함.

## 3. 데이터 모델 (Google Sheets)

모든 시트 1행은 헤더(고정, 코드에서 상수로 관리). 헤더 행은 고정(freeze)하고 보호.

### 3.1 `학생` 시트
| 열 | 타입 | 유효성 | 비고 |
|---|---|---|---|
| 학년도 | 숫자 | 4자리 | 예 2026 |
| 학년 | 숫자 | 1~6 (초) / 1~3 | 드롭다운 없음, 숫자 검증 |
| 반 | 숫자 | ≥1 | |
| 이름 | 텍스트 | 필수 | |
| 담임이름 | 텍스트 | | 반별 담임은 이 열에서 유추(가장 많이 적힌 값). 같은 반에 다른 값이 섞이면 업로드 미리보기·학생 화면에 경고 |
| 담임전화번호 | 텍스트 | 휴대폰·유선 | 위와 동일. 표시 형식 `담임 OOO 010-xxxx-xxxx`, 미등록이면 `담임 미등록` |
| 알레르기코드 | 텍스트 | `1~19` 를 쉼표 구분, 예 `1,2,6` | 공백 허용, 저장 시 정규화 |
| 기타알레르기 | 텍스트 | 자유 키워드, 쉼표 구분, 예 `키위,망고` | 메뉴명 포함 여부로 판별 |
| 비고 | 텍스트 | | |
| 학부모이메일 | 텍스트 | 이메일 형식 | 학부모알림=이메일 이면 필수 |
| 학부모연락처 | 텍스트 | 숫자·하이픈 | 학부모알림=문자 이면 필수 |
| 학부모알림 | 드롭다운 | `없음` / `이메일` / `문자` | 기본 `없음` |
| 사용여부 | 체크박스 | TRUE/FALSE | 기본 TRUE. FALSE 면 판별·알림 제외 |

- 열 순서(확정): 학년도, 학년, 반, 이름, 담임이름, 담임전화번호, 알레르기코드, 기타알레르기, 비고, 학부모이메일, 학부모연락처, 학부모알림, 사용여부.
  기존 시트는 setup 재실행 시 자동 마이그레이션(번호 열 삭제, 담임 열 삽입).
- 식별키: (학년도, 학년, 반, 이름). 같은 반 **동명이인은 허용하되 경고** — 업로드 시 파일 등장 순서 ↔ 시트 행 순서로 짝지어 수정하고 남는 행은 추가.
- 판별 대상 = `사용여부=TRUE` **이고** `학년도 = 현재 학년도` 인 학생.
  현재 학년도 = 3월 1일 기준 (1~2월은 전년도). 설정 시트에서 수동 override 가능.
- 담임: 해당 학생이 나오는 모든 곳(대시보드, 날짜 상세, 인쇄, xlsx 결과, 담당자 일일 메일)에 `담임 OOO 010-xxxx-xxxx` 를 붙인다.
  담임에게 직접 보내는 알림은 없음(이메일 없음). 문자 채널이 있을 때의 "담임 문자 알림"은 추후 옵션.

### 3.2 `급식` 시트
| 열 | 타입 | 비고 |
|---|---|---|
| 날짜 | 날짜 | `yyyy-MM-dd` |
| 식사구분 | 드롭다운 | `조식` / `중식` / `석식` |
| 메뉴명 | 텍스트 | 알레르기 번호 제거한 요리명 |
| 알레르기코드 | 텍스트 | `1,5,6` 형식. 빈칸 = **확인 필요** |
| 출처 | 드롭다운 | `NEIS` / `수동` |
| 수동수정여부 | 체크박스 | TRUE 면 재동기화 시 절대 덮어쓰지 않음 |
| 원본문자열 | 텍스트 | NEIS 원문 (예 `돈까스 (1.5.6.10)`) 디버깅용 |

- 1행 = 1메뉴. 고유키: (날짜, 식사구분, 메뉴명).
- 재동기화 규칙:
  - 해당 (날짜, 식사구분)에 `수동수정여부=TRUE` 행이 하나라도 있으면 그 끼니 전체를 건드리지 않음.
  - 그 외에는 해당 끼니의 NEIS 출처 행을 삭제 후 재삽입.
  - 수동으로 추가한 행(`출처=수동`)은 항상 보존.
  - 보호되어 건너뛴 (날짜, 끼니)는 UI(달력·날짜 상세·급식 관리)에 **"수동 보호"** 배지로 표시하고,
    **"NEIS 데이터로 되돌리기"** 버튼 제공: 해당 끼니의 모든 행을 삭제하고 NEIS 에서 다시 받아 채움 (확인 대화 후 실행).

### 3.3 `알림로그` 시트
| 열 | 비고 |
|---|---|
| 발송시각 | `yyyy-MM-dd HH:mm:ss` |
| 채널 | `이메일` / `문자` / `텔레그램` |
| 종류 | `담당자일일` / `담당자주간` / `동기화결과` / `학부모` |
| 수신자 | 이메일/번호/chat id (문자·번호는 일부 마스킹) |
| 대상일 | 알림이 다루는 급식 날짜 |
| 내용요약 | 100자 이내 |
| 성공여부 | TRUE/FALSE |
| 오류 | 실패 시 메시지 |
| 중복키 | `종류|대상일|수신자|학생키` — 중복 발송 방지에 사용 |

### 3.4 `설정` 시트 (키-값 2열: `키`, `값`, `설명`)
| 키 | 기본값 | 설명 |
|---|---|---|
| 학교명 | | schoolInfo 검색으로 채움 |
| 시도교육청코드 | | 예 `B10` |
| 학교코드 | | 예 `7010569` |
| 학년도override | (빈칸) | 비우면 자동 계산 |
| 관리할끼니 | `중식` | 쉼표 구분 복수 가능 `조식,중식` |
| 담당자알림시간 | `07:30` | HH:mm |
| 학부모알림시간 | `18:00` | 전날 HH:mm |
| 담당자이메일 | | 쉼표 구분 복수 |
| 담당자텔레그램chatid | | 쉼표 구분 복수 |
| 채널_이메일 | `TRUE` | |
| 채널_문자 | `FALSE` | 키가 있어야 켜짐 |
| 채널_텔레그램 | `FALSE` | 토큰이 있어야 켜짐 |
| 문자제공자 | `알리고` | `알리고` (1차 구현) / `솔라피` (추후, 동일 인터페이스) |
| 문자발신번호 | | 사전 등록된 번호 |
| 학부모알림사용 | `FALSE` | 전체 on/off |
| 담당자일일알림 | `TRUE` | |
| 담당자주간알림 | `TRUE` | 매주 월요일 |
| 주말·공휴일알림 | `FALSE` | 급식 없는 날은 스킵 |
| 웹앱URL | | 배포 후 자동 기록 (알림 링크용) |

- 비밀값(Script Properties, 시트에 저장 안 함): `NEIS_API_KEY`, `SMS_API_KEY`, `SMS_API_SECRET`,
  `TELEGRAM_BOT_TOKEN`, `APP_PASSWORD_HASH`, `APP_PASSWORD_SALT`.

### 3.5 학생 일괄 업로드 (엑셀 연동)
세 가지 입력 경로가 모두 **동일한 2차원 배열 → 검증 → 미리보기 → 확인 → 병합** 파이프라인을 탄다.
1. **.xlsx 파일 업로드** (웹앱): 브라우저에서 파일을 base64 로 전송 → 서버에서 Drive 고급 서비스로
   Google 시트로 변환(`Drive.Files.create` + `mimeType: application/vnd.google-apps.spreadsheet`) →
   첫 번째 시트 값 읽기 → 임시 파일 즉시 삭제. 크기 제한 5MB (google.script.run 페이로드 한도 고려).
2. **붙여넣기** (웹앱): 엑셀에서 복사한 탭 구분 텍스트를 textarea 에 붙여넣기 → 탭/줄바꿈으로 분리.
3. **`학생_업로드템플릿` 시트** (시트 메뉴 "템플릿 → 학생 반영"): 시트에 직접 붙여넣고 메뉴 실행.

- 헤더 인식: 1행이 `학생` 시트 헤더와 같으면 헤더로 취급(열 순서 무관, 헤더명으로 매핑). 헤더가 없으면
  `학생` 시트 열 순서로 간주. 필수 열: 학년, 반, 이름. 학년도가 비면 현재 학년도로 채움.
- 검증(오류): 코드 범위(1~19), 필수값, 이메일/전화 형식(학부모·담임), 학부모알림 값,
  학부모알림=이메일/문자인데 해당 연락처 없음.
- 경고(반영은 막지 않음): 같은 반 동명이인, 같은 반의 담임이름·전화번호 불일치.
- **병합 규칙 (삭제 없음)**: 현재 학년도 기존 학생과 **학년·반·이름이 같으면 "수정"**
  (담임·알레르기코드·기타알레르기·비고·학부모 정보를 파일 값으로 갱신, 사용여부 TRUE 로 복구), 아니면 "추가".
- 미리보기: 행별로 `추가` / `수정(변경 필드 표시)` / `변경 없음` / `오류(사유)` 배지 + ⚠ 경고 문구. 오류가 하나라도 있으면
  반영 버튼 비활성. 전체/오류만/경고만 필터. 확인 누르면 반영, 결과 요약(추가 n, 수정 n, 유지 n) 표시.
- **업로드용 양식 다운로드**: 헤더만 있는 xlsx (+ 2번째 시트에 알레르기 코드표 19종, 3번째 시트에 작성 안내).

### 3.6 xlsx 내보내기 (공통)
- 서버에서 임시 Google 시트 생성 → 값 기록 → Drive export(`application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`)
  로 xlsx blob → base64 로 클라이언트 전달 → 브라우저에서 다운로드. 임시 시트는 finally 에서 삭제.
  xlsx 는 Unicode 이므로 한글 깨짐 없음 (CSV 는 제공하지 않음).
- 내보내기 종류: ① 학생 명단(필터 반영) ② 특정 날짜 또는 월의 판별 결과(날짜·끼니·학생·메뉴·원인)
  ③ 반별 인쇄용 목록(반 × 날짜 표) ④ 급식 월 데이터(`급식` 시트 열 그대로) ⑤ 학생 업로드 양식.
- 파일명: `{종류}_{학교명}_{기간}.xlsx`.

### 3.7 급식 월 데이터 가져오기 (xlsx)
- `급식` 시트와 같은 헤더(날짜, 식사구분, 메뉴명, 알레르기코드 필수; 나머지 선택)의 xlsx 업로드.
- 검증: 날짜 형식·선택한 월 안인지, 식사구분 값, 코드 범위. 미리보기에 (날짜,끼니)별 메뉴 수와
  "기존 데이터를 대체함" 표시.
- 반영: 파일에 등장하는 **(날짜, 끼니) 조합만** 기존 행을 전부 삭제 후 파일 행으로 대체. 파일에 없는 날짜는 유지.
  가져온 행은 `출처=수동`, `수동수정여부=TRUE` 로 저장 → 이후 NEIS 재동기화에서 보호됨.

## 4. 알레르기 코드 (학교급식 표준 19종 = NEIS)

| 코드 | 명칭 | 코드 | 명칭 |
|---|---|---|---|
| 1 | 난류 | 11 | 복숭아 |
| 2 | 우유 | 12 | 토마토 |
| 3 | 메밀 | 13 | 아황산류 |
| 4 | 땅콩 | 14 | 호두 |
| 5 | 대두 | 15 | 닭고기 |
| 6 | 밀 | 16 | 쇠고기 |
| 7 | 고등어 | 17 | 오징어 |
| 8 | 게 | 18 | 조개류(굴·전복·홍합 포함) |
| 9 | 새우 | 19 | 잣 |
| 10 | 돼지고기 | | |

## 5. 급식 데이터 연동 (NEIS)

### 5.1 API
- 엔드포인트: `https://open.neis.go.kr/hub/mealServiceDietInfo`
- 필수 파라미터: `KEY`, `Type=json`, `ATPT_OFCDC_SC_CODE`, `SD_SCHUL_CODE`
- 기간: `MLSV_FROM_YMD`, `MLSV_TO_YMD` (yyyyMMdd), `MMEAL_SC_CODE`(1 조식, 2 중식, 3 석식)
- 페이징: `pIndex`, `pSize` (최대 1000). 한 달 × 끼니 1개면 1페이지로 충분.
- 응답 필드 (구현 전 open.neis.go.kr 공식 문서로 재확인 필수):
  `MLSV_YMD`, `MMEAL_SC_CODE`, `MMEAL_SC_NM`, `DDISH_NM`, `CAL_INFO`, `NTR_INFO`, `ORPLC_INFO` 등.
- `DDISH_NM` 은 `<br/>` 로 요리가 구분되고 각 요리 뒤에 `(1.5.6.)` 형태로 알레르기 번호가 붙음.
  형식 변형(`(1.5.6)`, `(1,5,6)`, 공백, 전각 괄호, 번호 없음)을 모두 파서 테스트로 커버.
- 데이터 없음: `RESULT.CODE = INFO-200`. 인증 오류·한도 초과 코드도 구분해 로그.
- 학교 검색: `https://open.neis.go.kr/hub/schoolInfo?KEY=&Type=json&SCHUL_NM=검색어`
  → 설정 화면에서 결과 목록 중 선택 → `ATPT_OFCDC_SC_CODE`, `SD_SCHUL_CODE`, `SCHUL_NM` 저장.

### 5.2 자동 동기화 스케줄
| 트리거 | 시각 | 동작 |
|---|---|---|
| 월간 | 매월 1일 04:00 | 이번 달 + 다음 달 가져오기, 결과 담당자에게 알림 |
| 일간 | 매일 03:00 | 이번 달 재동기화 (식단 변경 반영) |
| 수동 | 웹앱 버튼 | 지정 월 즉시 동기화 |

- 트리거 시각은 ±15분 오차 있음 (Apps Script 제약). 알림 시각과 겹치지 않도록 동기화는 새벽에 배치.
- 3.2 의 재동기화 규칙 적용. 변경 건수(추가/삭제/유지/보호됨) 를 반환·로그.
- API 실패 시 기존 데이터 유지, 오류를 알림로그 + 대시보드에 표시.

### 5.3 수동 입력
- 날짜 선택 → 해당 날짜의 끼니별 메뉴 목록 → 추가/수정/삭제.
- 알레르기 코드는 19개 체크박스로 입력. 수정 시 `수동수정여부=TRUE` 자동 설정.
- NEIS 에 알레르기 번호가 하나도 없는 메뉴는 UI 전반에 **"확인 필요"** 배지로 표시
  (김치·밥 등 실제로 없는 것과 누락을 사람이 구분해야 하므로 "확인함" 처리 기능 제공:
  확인 시 알레르기코드를 `-` 로 저장 = "없음 확인됨").

### 5.4 기타 알레르기 키워드 관리 목록
- 설정 `기타알레르기목록`: `키위=골드키위,그린키위; 망고; 복숭아=천도복숭아` (세미콜론 구분, `=` 뒤 동의어).
- 매칭: 학생 키워드가 목록에 있으면(주키워드 또는 동의어로) 그 항목의 모든 용어를 메뉴명에서 찾고, 없으면 **그 단어 그대로** 찾는다.
  보고되는 원인은 항상 학생이 적은 키워드.
- 학생 입력창(칩 입력)과 메뉴 편집창(메뉴명에서 감지된 키워드 표시)에서 자유 입력 + "목록에 추가"(`apiAddKeyword`) 가능.

## 6. 판별 로직 (순수 함수)

```
입력: students[] (활성 학생), meals[] (날짜·끼니별 메뉴)
출력: { [date]: { [mealType]: {
          menus: [{name, codes, needsCheck}],
          affected: [{ student, items: [{menu, matchedCodes[], matchedKeywords[]}] }],
          uncheckedMenus: [name...]
       } } }
```
- 코드 매칭: `student.codes ∩ menu.codes ≠ ∅`.
- 키워드 매칭: `menu.name` 에 `student.keywords` 중 하나라도 포함 (대소문자·공백 무시).
- 정렬: 학년 → 반 → 번호.
- 표시 형식: `3-2-15 홍길동 — 돈까스(밀, 돼지고기), 계란찜(난류)`.

## 7. 알림

### 7.1 채널 플러그인 인터페이스
```js
{ id: 'email'|'sms'|'telegram',
  isEnabled(settings, secrets): boolean,
  send({ to, subject, text, html? }): { ok: boolean, error?: string } }
```
- 이메일: `MailApp.sendEmail` (기본 ON). Gmail 무료 계정 일일 한도(약 100통)를 고려해 담당자 알림은 1통으로 묶음.
- 문자: 솔라피 **또는** 알리고 — 설정의 `문자제공자` 로 선택. 키·발신번호가 있어야 활성.
- 텔레그램: `sendMessage` API. 토큰 + chat id 있어야 활성. 담당자용(학부모 채널 아님).

### 7.2 담당자 알림 (기본 ON)
- **일일 요약**: 매일 `담당자알림시간`(±15분) 에 1통. 내용:
  1. 오늘 메뉴 + 알레르기 번호(명칭 병기)
  2. 해당 학생 목록 (학년-반-번호 이름 — 문제 메뉴(원인))
  3. 알레르기 표시 없는 메뉴("확인 필요") 목록
  4. 웹앱 링크
  - 급식 데이터가 없는 날(주말·방학·공휴일)은 `주말·공휴일알림=FALSE` 면 발송 안 함.
- **주간 요약**: 매주 월요일 같은 시각에 이번 주 날짜별 해당 인원 + 학생 목록.
- **동기화 결과**: 월간 동기화 후 성공/실패 및 건수. 실패 시 즉시.
- 이메일·텔레그램 중 켜진 채널 모두로 발송.

### 7.3 학부모 알림 (기본 OFF)
- `학부모알림사용=TRUE` 일 때만 동작. 전날 `학부모알림시간` 에 다음 급식일 기준으로 발송.
- 대상: 활성 학생 중 `학부모알림 ≠ 없음` 이고 다음날 해당 메뉴가 있는 학생만. 학생별 개별 발송.
- 채널: 학생의 `학부모알림` 값. 문자 채널이 비활성이면 **이메일로 대체 발송** + 로그에 `대체발송` 기록.
  이메일도 없으면 실패로 로그.
- 문구(짧게): `[OO초] 내일(9/1) 중식에 홍길동 학생이 못 먹는 메뉴가 있습니다: 돈까스(밀, 돼지고기)`
- 문자 90바이트 초과 시 LMS 로 전환(제공자 API 에 따름).

### 7.4 공통
- **중복 발송 방지**: 알림로그의 `중복키` 로 이미 성공한 건은 재발송 안 함 (트리거 중복 실행 대비).
- 모든 발송은 알림로그에 기록. 실패 건은 대시보드 상단에 표시(최근 7일 이내만).
  대시보드의 **"확인함 · 숨기기"** 를 누르면 그 시각(Script Properties `FAILURES_DISMISSED_AT`) 이전 실패는
  대시보드에서 사라진다. 로그 자체는 지우지 않으며(알림 로그 탭에서 계속 조회), 이후 새 실패는 다시 표시된다.
- 실행 시간 제한(6분) 고려: 학부모 발송은 루프마다 경과 시간 확인, 초과 시 중단하고 남은 건 로그.

## 8. UI (HtmlService 웹앱)

- 단일 페이지 앱(탭/라우팅), 한국어, 모바일 반응형. 외부 CDN 의존 최소화(Apps Script sandbox).
- 서버 호출은 `google.script.run` 으로. 모든 서버 함수는 세션 토큰 검사.

| 화면 | 내용 |
|---|---|
| 로그인 | 비밀번호 입력 → 서버 검증 → 세션 토큰(CacheService, **6시간** — CacheService 최대치. 요청마다 만료 연장) |
| 대시보드 | 오늘 해당 학생, 이번 주 날짜별 요약, 확인 필요 메뉴 수, 최근 알림 실패(숨기기 가능), 마지막 동기화 시각. **전광판** 버튼 |
| 해당 학생 목록 (공용) | 대시보드·날짜 상세·메뉴 편집 미리보기가 같은 컴포넌트. **반별 그룹** — 헤더 `1-1 · 담임 OOO 010-… · n명` + 접힌 상태에서는 이름 쉼표 요약, 클릭으로 펼치면 학생 카드(이름·비고, 원인 메뉴(알레르기), 학생 알레르기 칩 — 원인 강조). 카드 클릭 → 학생 수정 모달(재사용). 상단 "전체 펼치기/접기", 상태는 화면·끼니별로 sessionStorage 에 기억. 반이 하나거나 5명 이하면 기본 펼침 |
| 날짜 상세 메뉴 행 | 알레르기 칩은 이름 옆 최대 3개 + "+N"(툴팁 전체, 학생 관리 표와 같은 규칙). 수정·삭제 버튼은 마우스를 올리거나 행을 클릭했을 때만, "확인 필요" 행은 노란색 + "확인함" 버튼 항상 |
| 전광판 (`#/board`) | 헤더·탭 없이 오늘 해당 학생 **이름만** 전체 화면(F11 권장). 상단 한 줄: 날짜 · 끼니 · 해당 인원(+작은 시각). **행 = 반**(학년-반 순): 왼쪽 고정 200px 에 `1-1` 크게 + `담임 OOO` 작게, 오른쪽에 그 반 학생 **이름만** 같은 크기 카드로 가로 나열(넘치면 행 안에서 줄바꿈), 행 구분선. 화면 높이에 맞는 가장 큰 글자 배율을 자동 선택(보통 10개 반 이내는 한 화면), 최소 배율에서도 넘치면(30명 초과 등) 행 단위 페이지 전환 — **10초 간격, 1초 크로스페이드**(←/→/스페이스 수동). 5분마다 다시 받아 **바뀐 그룹만 교체**(전체 리렌더 없음). 예외 화면(상단 바 유지): 0명 → 중앙에 "오늘 해당 학생 없음" + 날짜, 급식 없음 → "급식 없음" + 날짜. 하단 구석에 아주 작은 상태(갱신 시각·페이지)와 ✕. Esc 로 나가기. 인쇄 화면과 별개, 주소로 바로 열 수 있음(바깥 해시 동기화). **데모**: `#/board?demo=normal|none|nomeal|paging` 은 가상 이름 데이터로 렌더(서버 호출·자동 갱신 없음, 상태 표시에 "데모"). 기준 스크린샷: `docs/board/` |
| 월간 달력 | 날짜 셀에 해당 인원 뱃지·확인 필요·수동 보호 표시. 클릭 → 날짜 상세 |
| 날짜 상세 | 끼니별 메뉴 + 알레르기 태그(번호+명칭) + 해당 학생 목록. 수동 편집 진입. 수동 보호 배지 + "NEIS 데이터로 되돌리기" |
| 학생 관리 | 검색/필터(학년·반), 추가/수정/삭제(사용여부 OFF), **일괄 업로드(xlsx 파일 / 붙여넣기 → 미리보기 → 확인 → 병합)**, **업로드용 양식 다운로드**, **학생 명단 xlsx 다운로드** |
| 급식 관리 | 수동 입력(5.3), 월 선택 즉시 동기화, 동기화 이력, 수동 보호 끼니 목록 + 되돌리기, **월 데이터 xlsx 내보내기/가져오기** |
| 결과 다운로드 | 날짜 상세·달력·인쇄용 화면에 **xlsx 다운로드** 버튼 (해당 날짜/월 판별 결과, 반별 인쇄용 목록) |
| 설정 | 학교 검색·선택, 끼니, 알림 시간(±15분 오차 안내 문구), 담당자 연락처, 채널 on/off, 비밀값 입력(마스킹, 저장 여부만 표시), 비밀번호 변경, 테스트 발송 버튼 |
| 알림 로그 | 최근 N건, 실패만 필터 |
| 인쇄용(반별) | 날짜/기간 + 반 선택 → 학생·메뉴·원인 표. `@media print` 최적화 |

## 9. 배포·운영

- 프로젝트 생성: `clasp create --type sheets --title "급식 알레르기 판별" --rootDir src` 로
  시트 + 바인딩된 스크립트를 한 번에 생성 (scriptId 수작업 불필요).
- 웹앱 배포: "실행 사용자 = 나(배포 계정)", "액세스 = 링크가 있는 모든 사용자". 접근 제어는 앱 비밀번호로.
- **접속 비밀번호 모드** (Script Properties `AUTH_MODE`, 시트 메뉴 **접속 비밀번호** 서브메뉴에서만 변경, 웹앱 설정 화면에는 없음):
  - `off` (기본): 로그인 없이 바로 대시보드.
  - `settings`: 대시보드·달력·학생·급식·인쇄·로그는 자유, **설정 탭(설정 계열 API)만** 비밀번호.
  - `on`: 전체 로그인 필요.
  - 서버는 `requireSession(token, level)` 로 판정 — 설정 계열 API 는 `level='admin'`. 모드 변경은 재배포 없이 즉시 적용.
- **웹앱 URL 고정**: 최초 1회 `clasp deploy -d "v1"` 로 배포 ID 를 얻은 뒤, 이후 업데이트는 항상
  `clasp deploy -i <deploymentId> -d "설명"` 로 **같은 배포 ID 에 재배포**. 새 배포를 만들면 URL 이 바뀜.
  README 와 `deploy` npm 스크립트에 명시. 배포 ID 는 `.clasp.json` 옆 `.deployment-id`(gitignore) 에 보관.
- `setup()` 1개 함수 (시트 커스텀 메뉴 **[급식 알레르기] → 초기 설정** 에서 실행):
  시트 생성/헤더/유효성/보호 → 설정 기본값 → 트리거 등록(기존 트리거 정리 후) → 초기 비밀번호 안내.
  여러 번 실행해도 안전.
- 커스텀 메뉴 항목: 초기 설정, 지금 동기화, 오늘 알림 테스트, 웹앱 열기, 템플릿 → 학생 반영,
  **접속 비밀번호 재설정**(새 랜덤 비밀번호 생성 → 알림창에 1회 표시 → 기존 세션 전부 무효화),
  **이관용 설정 내보내기 / 가져오기**, **이 계정의 트리거 모두 해제**(이관 후 구 계정에서 실행).

### 9.1 다른 구글 계정으로 이관 (목표 15분)
- 원리: 시트 **사본 만들기**는 바인딩된 Apps Script 코드까지 복사하지만,
  **Script Properties(비밀값), 트리거, 웹앱 배포는 복사되지 않는다.** 이 세 가지를 새 계정에서 다시 만든다.
- **이관용 설정 내보내기**: 설정 시트 전체 + 비밀값이 아닌 Script Properties(마지막 동기화 시각 등)를
  JSON 으로 만들어 모달(복사 가능한 텍스트)에 표시. 비밀값(`NEIS_API_KEY`, `SMS_*`, `TELEGRAM_*`, `APP_PASSWORD_*`)은
  **절대 포함하지 않고**, 대신 "어떤 비밀값이 설정되어 있었는지" 키 이름 목록만 포함해 새 계정에서 재입력 체크리스트로 쓴다.
- **이관용 설정 가져오기**: JSON 붙여넣기 → 스키마 검증 → 설정 시트 덮어쓰기(확인 후) + 비밀값 재입력 체크리스트 표시.
  사본에는 설정 시트가 이미 있으므로 주로 "새로 만든 빈 프로젝트로 옮기는 경우"와 검증용.
- 절차 (README 에 그대로 수록):
  1. 구 계정: 메뉴 → 이관용 설정 내보내기 → JSON 복사해 둠. 시트를 새 계정에 편집자로 공유.
  2. 새 계정: 시트 열기 → 파일 → 사본 만들기 (내 드라이브). 사본을 연다.
  3. 새 계정: 메뉴 → 초기 설정 → 권한 승인. (트리거가 새 계정 소유로 등록되고 새 비밀번호가 표시됨)
  4. 새 계정: 메뉴 → 이관용 설정 가져오기 → JSON 붙여넣기 (설정 확인 + 비밀값 체크리스트).
  5. 새 계정: 확장 프로그램 → Apps Script → 배포 → 새 배포 → 웹앱 → URL 확보 → 웹앱 설정 화면에서 비밀값 재입력.
  6. 구 계정: 메뉴 → 이 계정의 트리거 모두 해제 (중복 알림 방지). 웹앱 배포 보관 처리. 사용자에게 새 URL 공지.
  - 로컬 clasp 를 계속 쓰려면 새 계정으로 `clasp login` 후 `clasp clone-script <새 scriptId> --rootDir src`.
  - 웹앱 URL 은 스크립트가 달라지므로 **반드시 바뀐다**. README 에 명시.
- README.md: 구글 계정 생성 → Node/clasp 설치 → `clasp login` → Apps Script API 켜기 → `clasp create` →
  `clasp push` → setup 실행 → 권한 승인 → NEIS 키 발급 → 웹앱 배포(배포 ID 고정) → 설정 화면 → 테스트 순서.
  트리거 ±15분 오차, 배포 ID 재사용 규칙 명시. 스크린샷 위치 표시.

## 10. 테스트

- `test/` 아래 Node 내장 `node:test` 로 순수 로직 테스트:
  NEIS 문자열 파싱(변형 케이스), 코드 정규화·검증, 판별(코드/키워드/비활성/학년도), 알림 문구,
  학생 업로드 검증, 재동기화 diff 계산, 중복키 생성.
- Apps Script 의존 코드는 얇게 유지하고 수동 체크리스트로 검증.

## 11. 미결/결정 사항

| 항목 | 결정 |
|---|---|
| 문자 제공자 우선 구현 | **알리고** 먼저 (확정). 솔라피는 동일 채널 인터페이스로 추후 추가 |
| 웹앱 URL 표기 | Workspace 계정의 `ScriptApp.getService().getUrl()` 은 조직 전용 `https://script.google.com/a/macros/{도메인}/s/{id}/exec` 를 돌려주는데 조직 밖에서는 열리지 않는다. `normalizeWebAppUrl`(core) 로 항상 `https://script.google.com/macros/s/{id}/exec` 로 바꿔 저장(`WEBAPP_URL`, 설정 `웹앱URL`)·표시(메뉴 웹앱 열기, 설정 화면)·알림 링크에 쓴다. 또한 시트 메뉴·setup 컨텍스트의 `getUrl()` 은 HEAD(/dev) 배포를 가리킬 수 있으므로, **실제 웹 요청(`doGet`)에서 확인된 배포 ID** 를 `DEPLOYMENT_ID` 상태에 저장하고 URL 은 항상 이 ID 로 조립한다(`buildWebAppUrl`). 이관 시 `DEPLOYMENT_ID` 는 가져오지 않는다 (2026-09-01) |
| 대시보드 실패 알림 숨기기 | 로그 행을 지우거나 표시하지 않고, **숨긴 시각**(`FAILURES_DISMISSED_AT`) 하나만 저장해 그 이전 실패를 걸러낸다. 개별 숨김 대신 "지금까지 전부 확인함" 한 버튼 — 시트 열 추가·마이그레이션 없이 끝나고 운영자가 실제로 원하는 동작이기 때문. 추가로 7일 지난 실패는 대시보드에서 자동 제외 (2026-09-01) |
| 비밀번호 저장 | SHA-256 + salt 해시 (Script Properties). 초기 비밀번호는 setup 시 랜덤 생성 후 시트 메뉴 알림창에 1회 표시. 메뉴에서 재설정 가능 |
| 세션 | CacheService 토큰, **6시간**(CacheService 상한). 요청마다 갱신 |
| 현재 학년도 | 3/1 기준 자동, 설정에서 override. 판별 대상 = 사용여부 ∧ 현재 학년도 (확정) |
| 수동 보호 범위 | 끼니 단위 보호 (확정). UI 배지 + "NEIS 데이터로 되돌리기" 제공 |
| "확인 필요" 해소 | 알레르기코드 `-` = 없음 확인됨 |
| 웹앱 URL 고정 | 항상 `clasp deploy -i <id>` 로 같은 배포 ID 재사용 |
| UI 프레임워크 | 없음 (순수 HTML/CSS/JS) |
| xlsx 처리 위치 | 서버(Drive 고급 서비스 변환/export). 클라이언트 라이브러리(SheetJS 등) 불사용. `drive.file` 스코프 + Drive v3 고급 서비스 필요 |
| 학생 병합 키 | 학년·반·이름 일치 = 수정. 동명이인은 순서대로 짝지어 수정 + 경고. 삭제 없음 |
| 담임 정보 | 학생 시트의 담임이름·담임전화번호 열에서 반별 유추(최빈값). 불일치는 경고. 담임 직접 알림 없음 |
| UI 이벤트 | 화면 컨테이너 이벤트는 `App.on(el, type, fn)` 교체 방식만 사용(누적 방지). 모달은 한 번에 하나(확인창만 겹침 허용) |
| 급식 xlsx 가져오기 | 파일에 있는 (날짜,끼니)만 대체, 출처=수동·수동수정여부=TRUE 로 저장 |
